{
  "name": "plugin-metric-cloudwatch",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Ale Plugin Metric Cloudwatch",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "private": true,
  "scripts": {
    "dev": "npm run build && LOG_LEVEL=info aled dev",
    "build": "rimraf lib && tsc",
    "watch": "npm run build && tsc -w --preserveWatchOutput",
    "lint": "prettier --write ."
  },
  "devDependencies": {
    "@ale-run/runtime": "^0.1.0-rc.0",
    "@types/node": "^22.5.1",
    "cross-env": "^7.0.2",
    "prettier": "^2.0.4",
    "ts-node-dev": "^2.0.0",
    "tslint": "^6.1.3",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.716.0",
    "ansi-colors": "^4.1.3"
  }
}
